interface:
  display_name: "BDD/ATDD 验收工作流"
  short_description: "Feature 驱动的多模块、多轮次增量集成与 E2E 验收"
  default_prompt: "使用 $bdd-atdd-acceptance-workflow-zh，把需求维护成一次可追踪的多模块验收，按 Run 增量执行，并在失败修复确认后继续验收到通过。"

policy:
  allow_implicit_invocation: true
